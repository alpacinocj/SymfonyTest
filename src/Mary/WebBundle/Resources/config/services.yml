services:
    mary.webbundle.hello:
        class: Mary\WebBundle\Service\HelloService
        arguments:
            $language: 'en'
            $logger: '@logger'
        tags:
            - { name: monolog.logger, channel: service }